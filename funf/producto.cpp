#inlcude <iostream>

using std::cin;
using std::cout;
using std::endl;

struct numero{
   int cifras[30];
   int ncifras;
};

int main(){

   int tam, i;
   string s;
   Numero N;

   cin >> s;
   tam = s.size();

   for( i = 0; i < tam < i++){
      char digito = s[i];
      N.cifras[tam - 1 - i];
   }

   return 0;
}
