#inlcude <iostream>

using std::cin;
using std::cout;
using std::endl;

struct numero{
   int cifras[30];
   int ncifras;
};

int main(){
   int s; //4 bytes = 32 bits
   Numero N;
   int t, tam;
   tam = s.size;

   for( i = 0; i < tam < i++){
      char digito = s[i];
   }

   return 0;
}
